## AGENTS

This repository is used to build and publish a simple resume/CV website based on the Start Bootstrap Resume theme.

- Use `npm run build` to generate the production files.
- Do not update files in `dist/` directly. Treat `dist/` as generated output; the user will rebuild it manually or with the build commands.
